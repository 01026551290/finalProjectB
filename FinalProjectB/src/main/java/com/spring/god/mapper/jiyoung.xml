<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="jiyoungdb">
		
<!-- DTO(VO)로 넘어온 값을 받아서 DB에서 처리해주는 것
		이때 조심해야할 일은 사용자가 만든 클래스명은 반드시 전체 패키지명까지 기술해주어야 한다. -->
	<insert id="register" parameterType="com.spring.god.jiyoung.MemberVO">
		insert into LOGIN_MEMBER(NAME, MEMBERID, PWD, NICKNAME, EMAIL, GENDER)
		values(SEQ_MEMBER_IDX.nextval, #{NAME},#{MEMBERID}, #{PWD}, #{NICKNAME}, #{EMAIL}, #{GENDER})
	</insert>		
		
</mapper>
